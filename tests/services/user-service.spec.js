describe('app/services/users', () => {
  test('getAll', () => {
    expect(0).toEqual(0);
  });
  test('getUser', () => {
    expect(0).toEqual(0);
  });
  test('createUser', () => {
    expect(0).toEqual(0);
  });
  test('loginUser', () => {
    expect(0).toEqual(0);
  });
});
